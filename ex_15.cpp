// Leia uma string s1 e um número inteiro n (n > 0). Em seguida, gere a string s2 com os n primeiros
// caracteres de s1 e imprima s2.
#include <stdio.h>
#include <string.h>
const int MAX_LEN = 100;

int main()
{
    char s1[MAX_LEN], s2[MAX_LEN];
    int n;

    printf("Digite uma string: ");
    gets(s1);

    printf("Digite a posicao n: ");
    scanf("%d", &n);

    //mover todos os  caracteres de s2 n posições para a direita

    //colocar os n primeiros caracteres de s1 nas n primeiras posições de s2
}